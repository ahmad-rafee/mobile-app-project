import 'package:flutter/material.dart';

class Stat {
  final String title;
  final String value;
  final IconData icon;
  final int page;
  final Color color;

  Stat(this.title, this.value, this.icon, this.page, this.color);
}
